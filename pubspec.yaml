name: otsp_attendance
description: "BACT Test Photo Capture Application"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  # GetX - State Management & Routing
  get: ^4.6.6
  get_storage: ^2.1.1
  
  # Network & API
  dio: ^5.4.0
  connectivity_plus: ^5.0.2
  
  # Camera & Images
  camera: ^0.10.5+5
  image_picker: ^1.0.7
  path_provider: ^2.1.2
  
  # UI
  cupertino_icons: ^1.0.8
  
  # Utilities
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # Uncomment these when we add assets later
  # assets:
  #   - assets/images/